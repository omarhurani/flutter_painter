import 'painter_event.dart';

/// An event representing the select object drawable being deleted.
class SelectedObjectDrawableRemovedEvent extends PainterEvent {
  /// Creates an [SelectedObjectDrawableRemovedEvent].
  const SelectedObjectDrawableRemovedEvent();
}
